% Generated from measured evaluation output — do NOT hand-edit.
% Source: hcp_project/outputs/eval_real_20260910_163553.json
%
% This file previously contained fabricated benchmark numbers
% (minADE_5 0.81 vs 0.78, 32.5ms vs 115.2ms, 76% pruning, 98.5% accuracy
% retention) that were never produced by any code in this repository.
% They have been replaced with what evaluate.py actually measured.
\begin{table}[h]
\centering
\caption{Measured trajectory prediction and latency metrics, HCP on vs.\ off.
Evaluated on official nuScenes val split over 2,000 agent trajectories.
The model was trained without a held-out validation split, so these are not
generalisation figures.}
\label{tab:benchmarks}
\begin{tabular}{lcccc}
\toprule
\textbf{Configuration} & \textbf{minADE} $\downarrow$ & \textbf{minFDE} $\downarrow$ & \textbf{MR@2m} $\downarrow$ & \textbf{ms/agent} $\downarrow$ \\
\midrule
No-HCP (MTR baseline) & 25.82 & 25.17 & 0.989 & 8.59 \\
HCP + MTR             & 25.82 & 25.17 & 0.989 & 16.29 \\
\bottomrule
\end{tabular}
\end{table}

% Note on the identical accuracy columns: the HCP mask is applied only to the
% decoder's confidence logits, while minADE/minFDE/MR take the minimum over all
% modes and ignore confidence entirely. The two configurations are therefore
% mathematically required to produce the same accuracy figures; this is not
% evidence that pruning preserves accuracy.
